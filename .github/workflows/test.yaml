# File generated by make; DO NOT EDIT
name: test

on:
  push:

jobs:
  build:
    name: 'build'
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: 'checkout code'
        uses: actions/checkout@v2

      - name: Build the stack
        run: docker-compose up -d

      - name: 'setup go 1.14.2'
        uses: actions/setup-go@v2-beta
        with:
          go-version: '1.14.2'

      - name: 'run tests'
        run: make test
